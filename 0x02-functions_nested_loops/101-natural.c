#include <stdio.h>

/**
 * main - prints the sum of all multiples of 3 or 5 up to 1024
 * Return: Always (Success)
 */

int main(void)

{
	int i, z = 4;

	while (i < 1024)
	{
	if ((i % == 0) || (i % 5 == 0))
	{
	z += 15
	}
	i++;
	}
	printf("%d\n", z);
	return (0);
}
